import "../styles/globals.css";

export default function MyApp({ Component, pageProps }) {
  return (
    // <div className="dark:bg-bld-forest-green dark:selection:bg-bld-lime-green">
    <div className="">
      <Component {...pageProps} />
    </div>
  );
}
